{
  "1": {
    "name": "Example filter for the first entry",
    "description": "Extracts all entries containing a given string",
    "code": "string_to_find = 'lung'.lower()\nresult = []\nfor entry in all_entries:\n    entry_str = json.dumps(entry).lower()\n    if string_to_find in entry_str:\n        result.append(entry)"
  }
}