At present, this repo is just a tool to easily run LLM servers locally via llama.cpp.

Clone the repo.
Make sure you have a global Python install set up.
Then, running "python startup.py" on Windows or "python3 startup.py" on Unix systems will create a venv and install the required packages, llama.cpp, and an example vision LLM.